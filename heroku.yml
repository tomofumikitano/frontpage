setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    common: Dockerfile
    # web: Dockerfile.web
    # crawler: Dockerfile.crawler
run:
  web: 
    image: common
    command:
      - /entrypoint.web.sh
  crawler: 
    image: common
    command:
      - /entrypoint.crawler.sh
