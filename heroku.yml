setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: Dockerfile.web
    crawler: Dockerfile.crawler
# run:
#   web: /entrypoint.web.sh
#   crawler: /entrypoint.crawler.sh
