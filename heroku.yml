setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: Dockerfile
    crawler: Dockerfile
run:
  web: 
    command:
      - sh /entrypoint.web.sh
  crawler:
    command:
      - sh /entrypoint.crawler.sh
